using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// This class only exists to distinguish the different types of health
// The same thing could be accomplished with enums, tags, etc, but this is set up
// to optimize the raycasting process
public class EnemyHealth : Health {
    //// Start is called before the first frame update
    //void Start() {
    //    base.Start();
    //}

    //// Update is called once per frame
    //void Update() {

    //}
}
